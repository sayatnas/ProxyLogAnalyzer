# Copy to .env and fill in. .env is gitignored and must never be committed.

JWT_SECRET=random-string
OPENAI_API_KEY=

# Demo account created on first run
DEMO_USER=analyst
DEMO_PASSWORD=analyst
OPENAI_REASONING_EFFORT=
